# Contributing

Keep changes small, source-aware, and compatible with D35E data structures.

- Do not present Pathfinder-derived defaults as D&D 3.5 RAW.
- Keep called-shot outcomes configurable.
- Avoid writing live world data on module load.
- Add tests for pure data logic.
- Run `npm run validate` before submitting changes.
